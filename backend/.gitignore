# Environment and secrets
.env
.env.*
!.env.example
secrets/*
!secrets/.gitkeep
!secrets/*.example

# Build artifacts
/bin/
/tmp/

# IDE
.idea/
.vscode/

# Test
coverage.out

# Swagger — track docs.go (needed for build), exclude generated JSON/YAML blobs
# Uncomment the lines below if you prefer not to commit the generated spec files.
# docs/swagger.json
# docs/swagger.yaml
